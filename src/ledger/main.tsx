import ReactDOM from 'react-dom/client'
import '../../fonts.css'
import { Ledger } from 'ledger/views/ledger'
import './../pairing/style.css'

ReactDOM.createRoot(document.getElementById('root')!).render(<Ledger />)
